import { BorderClassPipe } from './border-class.pipe';

describe('BorderClassPipe', () => {
  it('create an instance', () => {
    const pipe = new BorderClassPipe();
    expect(pipe).toBeTruthy();
  });
});
